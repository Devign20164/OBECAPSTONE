{
  "name": "pet-website-backend",
  "version": "1.0.0",
  "description": "Backend for pet website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch '*.js' --watch 'routes/*' --watch 'controllers/*' --watch 'models/*' --watch 'config/*' server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
} 